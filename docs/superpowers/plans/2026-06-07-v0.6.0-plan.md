# v0.6.0 重构实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 GLUT 课表 App 从单页切换重构为 Navigation Drawer 布局，用直接登录替代 WebView，新增考试成绩查询、更新检查、刷新反馈。

**Architecture:** Material 3 `ModalNavigationDrawer` + `DrawerItem` enum 进行页面切换。直接 HTTP 登录（复用 `AcademicLoginService`）替代 WebView。新增 `ScoreEntity` + `ScoreDao` 存入 Room。新增 `ScoreParser` 解析教务成绩 HTML。新增 `UpdateChecker` 检查 GitHub Releases。

**Tech Stack:** Kotlin, Jetpack Compose, Material 3, Room, OkHttp, DataStore

---

### Task 1: 新增 ScoreEntity 和 ScoreInfo 数据模型

**Files:**
- Modify: `app/src/main/java/com/glut/schedule/data/local/ScheduleEntities.kt`
- Create: `app/src/main/java/com/glut/schedule/data/model/ScoreModels.kt`

- [ ] **Step 1: 在 ScheduleEntities.kt 末尾添加 ScoreEntity**

```kotlin
@Entity(tableName = "scores")
data class ScoreEntity(
    @PrimaryKey val id: String,
    val courseName: String,
    val score: String,
    val gpa: Double,
    val credit: Double,
    val year: String,
    val term: Int,
    val category: String,
    val examType: String
)
```

- [ ] **Step 2: 创建 ScoreModels.kt**

```kotlin
package com.glut.schedule.data.model

import com.glut.schedule.data.local.ScoreEntity
import java.security.MessageDigest

data class ScoreInfo(
    val id: String,
    val courseName: String,
    val score: String,
    val gpa: Double,
    val credit: Double,
    val year: String,
    val term: Int,
    val category: String,
    val examType: String
) {
    companion object {
        fun stableId(courseName: String, year: String, term: Int): String {
            val raw = "$courseName|$year|$term"
            return MessageDigest.getInstance("MD5")
                .digest(raw.toByteArray(Charsets.UTF_8))
                .joinToString("") { "%02x".format(it) }
        }
    }
}

fun ScoreInfo.toEntity(): ScoreEntity = ScoreEntity(
    id = id, courseName = courseName, score = score,
    gpa = gpa, credit = credit, year = year,
    term = term, category = category, examType = examType
)

fun ScoreEntity.toModel(): ScoreInfo = ScoreInfo(
    id = id, courseName = courseName, score = score,
    gpa = gpa, credit = credit, year = year,
    term = term, category = category, examType = examType
)
```

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/glut/schedule/data/local/ScheduleEntities.kt app/src/main/java/com/glut/schedule/data/model/ScoreModels.kt
git commit -m "feat: add ScoreEntity and ScoreInfo data models"
```

---

### Task 2: 更新 Room Database 和 DAO 支持成绩

**Files:**
- Modify: `app/src/main/java/com/glut/schedule/data/local/ScheduleDatabase.kt`
- Modify: `app/src/main/java/com/glut/schedule/data/local/ScheduleDao.kt`

- [ ] **Step 1: 在 ScheduleDatabase.kt 添加 ScoreEntity，版本升到 3**

```kotlin
@Database(
    entities = [
        CourseEntity::class, CourseOccurrenceEntity::class,
        ClassPeriodEntity::class, ExamEntity::class,
        ScoreEntity::class
    ],
    version = 3,
    exportSchema = false
)
abstract class ScheduleDatabase : RoomDatabase {
    abstract fun scheduleDao(): ScheduleDao
}
```

- [ ] **Step 2: 在 ScheduleDao.kt 添加成绩 DAO 方法**

```kotlin
@Query("SELECT * FROM scores ORDER BY year DESC, term DESC, courseName")
fun observeScores(): Flow<List<ScoreEntity>>

@Query("SELECT COUNT(*) FROM scores")
suspend fun scoreCount(): Int

@Insert(onConflict = OnConflictStrategy.REPLACE)
suspend fun insertScores(scores: List<ScoreEntity>)

@Query("DELETE FROM scores")
suspend fun deleteAllScores()

@Transaction
suspend fun replaceScores(scores: List<ScoreEntity>) {
    deleteAllScores()
    insertScores(scores)
}
```

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/glut/schedule/data/local/ScheduleDatabase.kt app/src/main/java/com/glut/schedule/data/local/ScheduleDao.kt
git commit -m "feat: update Room database and DAO for score support"
```

---

### Task 3: 更新 ScheduleRepository 支持成绩

**Files:**
- Modify: `app/src/main/java/com/glut/schedule/data/repository/ScheduleRepository.kt`

- [ ] **Step 1: 添加成绩方法**

```kotlin
import com.glut.schedule.data.model.ScoreInfo

// In class body:
val scores: Flow<List<ScoreInfo>> = dao.observeScores().map { entities ->
    entities.map { it.toModel() }
}

suspend fun replaceScores(scores: List<ScoreInfo>) {
    dao.replaceScores(scores.map { it.toEntity() })
}
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/glut/schedule/data/repository/ScheduleRepository.kt
git commit -m "feat: add score methods to ScheduleRepository"
```

---

### Task 4: 新增成绩解析器 ScoreParser

**Files:**
- Create: `app/src/main/java/com/glut/schedule/service/parser/ScoreParser.kt`

- [ ] **Step 1: 创建 ScoreParser**

```kotlin
package com.glut.schedule.service.parser

import com.glut.schedule.data.model.ScoreInfo

class ScoreParser {
    fun parseScoreHtml(html: String, year: String, term: Int): List<ScoreInfo> {
        val scores = mutableListOf<ScoreInfo>()
        val body = html.replace(Regex("""\s+"""), " ")

        val tableRegex = Regex(
            """<table[^>]*class\s*=\s*["'][^"']*datalist[^"']*["'][^>]*>(.*?)</table>""",
            RegexOption.DOT_MATCHES_ALL
        )
        val tableHtml = tableRegex.find(body)?.groupValues?.get(1) ?: body

        val rowRegex = Regex("""<tr[^>]*>(.*?)</tr>""", RegexOption.DOT_MATCHES_ALL)
        val rows = rowRegex.findAll(tableHtml).toList()

        val headerIndex = rows.indexOfFirst { it.value.contains("<th") }
        val dataRows = if (headerIndex >= 0) rows.drop(headerIndex + 1) else rows.drop(1)

        for (row in dataRows) {
            val cells = parseCells(row.value)
            if (cells.isEmpty()) continue
            val courseName = cleanHtmlText(cells.getOrElse(4) { "" })
            if (courseName.isBlank()) continue

            val scoreText = cleanHtmlText(cells.getOrElse(7) { "" })
            val category = cleanHtmlText(cells.getOrElse(1) { "" })
            val credit = cells.getOrElse(4) { "0" }.let {
                Regex("""[.\d]+""").find(it)?.value?.toDoubleOrNull() ?: 0.0
            }
            val examType = cleanHtmlText(cells.getOrElse(2) { "" })
            val gpa = scoreToGpa(scoreText)

            if (courseName.isNotBlank() && scoreText.isNotBlank()) {
                scores.add(ScoreInfo(
                    id = ScoreInfo.stableId(courseName, year, term),
                    courseName = courseName, score = scoreText,
                    gpa = gpa, credit = credit, year = year,
                    term = term, category = category, examType = examType
                ))
            }
        }
        return scores
    }

    private fun parseCells(rowHtml: String): List<String> {
        val cellRegex = Regex("""<t[dh][^>]*>(.*?)</t[dh]>""", RegexOption.DOT_MATCHES_ALL)
        return cellRegex.findAll(rowHtml).map { it.groupValues[1] }.toList()
    }

    private fun cleanHtmlText(text: String): String {
        return text
            .replace(Regex("""<[^>]+>"""), "")
            .replace(Regex("&nbsp;|&#160;|&#xA0;", RegexOption.IGNORE_CASE), " ")
            .replace(Regex("&amp;", RegexOption.IGNORE_CASE), "&")
            .replace(Regex("&lt;", RegexOption.IGNORE_CASE), "<")
            .replace(Regex("&gt;", RegexOption.IGNORE_CASE), ">")
            .trim()
    }

    private fun scoreToGpa(scoreText: String): Double {
        val gradeMap = mapOf(
            "优秀" to 4.0, "优" to 4.0, "A" to 4.0,
            "良好" to 3.0, "良" to 3.0, "B" to 3.0,
            "中等" to 2.0, "中" to 2.0, "C" to 2.0,
            "及格" to 1.0, "D" to 1.0,
            "不及格" to 0.0, "F" to 0.0
        )
        gradeMap.entries.firstOrNull { (key, _) ->
            scoreText.contains(key, ignoreCase = true)
        }?.value ?: run {
            val numeric = scoreText.replace(Regex("""[^\d.]"""), "").toDoubleOrNull()
            when {
                numeric == null -> 0.0
                numeric >= 90 -> 4.0
                numeric >= 85 -> 3.7
                numeric >= 80 -> 3.3
                numeric >= 75 -> 3.0
                numeric >= 70 -> 2.7
                numeric >= 65 -> 2.3
                numeric >= 60 -> 2.0
                numeric >= 1 -> 1.0
                else -> 0.0
            }
        }
    }
}
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/glut/schedule/service/parser/ScoreParser.kt
git commit -m "feat: add ScoreParser for academic grade HTML parsing"
```

---

### Task 5: 新增 App 更新检查

**Files:**
- Create: `app/src/main/java/com/glut/schedule/service/UpdateChecker.kt`

- [ ] **Step 1: 创建 UpdateChecker**

```kotlin
package com.glut.schedule.service

import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext
import okhttp3.OkHttpClient
import okhttp3.Request
import org.json.JSONObject
import java.util.concurrent.TimeUnit

data class UpdateInfo(
    val latestVersion: String,
    val downloadUrl: String,
    val releaseNotes: String,
    val isNewer: Boolean
)

class UpdateChecker(
    private val repoOwner: String = "hzhkdh",
    private val repoName: String = "glut-schedule"
) {
    private val client = OkHttpClient.Builder()
        .connectTimeout(10, TimeUnit.SECONDS)
        .readTimeout(10, TimeUnit.SECONDS)
        .build()

    suspend fun check(currentVersion: String): UpdateInfo? = withContext(Dispatchers.IO) {
        runCatching {
            val request = Request.Builder()
                .url("https://api.github.com/repos/$repoOwner/$repoName/releases/latest")
                .header("Accept", "application/vnd.github+json")
                .header("User-Agent", "GlutSchedule/$currentVersion")
                .get().build()

            client.newCall(request).execute().use { response ->
                if (!response.isSuccessful) return@runCatching null
                val json = JSONObject(response.body?.string() ?: return@runCatching null)
                val tagName = json.optString("tag_name", "").removePrefix("v")
                val htmlUrl = json.optString("html_url", "")
                val notes = json.optString("body", "")
                UpdateInfo(
                    latestVersion = tagName,
                    downloadUrl = htmlUrl,
                    releaseNotes = notes,
                    isNewer = compareVersions(tagName, currentVersion) > 0
                )
            }
        }.getOrNull()
    }

    private fun compareVersions(a: String, b: String): Int {
        val partsA = a.split(".").map { it.toIntOrNull() ?: 0 }
        val partsB = b.split(".").map { it.toIntOrNull() ?: 0 }
        for (i in 0 until maxOf(partsA.size, partsB.size)) {
            val va = partsA.getOrElse(i) { 0 }
            val vb = partsB.getOrElse(i) { 0 }
            if (va != vb) return va.compareTo(vb)
        }
        return 0
    }
}
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/glut/schedule/service/UpdateChecker.kt
git commit -m "feat: add UpdateChecker for GitHub Releases"
```

---

### Task 6: 创建 DrawerItem 导航枚举

**Files:**
- Create: `app/src/main/java/com/glut/schedule/ui/navigation/DrawerItem.kt`

- [ ] **Step 1: 创建 DrawerItem**

```kotlin
package com.glut.schedule.ui.navigation

import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.outlined.Assessment
import androidx.compose.material.icons.outlined.CalendarMonth
import androidx.compose.material.icons.outlined.Download
import androidx.compose.material.icons.outlined.EventNote
import androidx.compose.material.icons.outlined.Info
import androidx.compose.material.icons.outlined.Settings
import androidx.compose.ui.graphics.vector.ImageVector

enum class DrawerItem(val title: String, val icon: ImageVector) {
    Schedule("课程表", Icons.Outlined.CalendarMonth),
    Score("考试成绩", Icons.Outlined.Assessment),
    Exam("考试安排", Icons.Outlined.EventNote),
    Import("导入课表", Icons.Outlined.Download),
    Settings("设置", Icons.Outlined.Settings),
    About("关于", Icons.Outlined.Info);
}
```

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/glut/schedule/ui/navigation/DrawerItem.kt
git commit -m "feat: add DrawerItem enum for navigation drawer"
```

---

### Task 7: 创建 AboutScreen

**Files:**
- Create: `app/src/main/java/com/glut/schedule/ui/pages/AboutScreen.kt`

- [ ] **Step 1: 创建 AboutScreen**

从 ScheduleScreen 的 AboutScheduleDialog 抽离并扩展为独立页面。包含：App 图标、名称、版本号卡片（点击检查更新）、开源许可、GitHub 链接、底部署名。

（完整代码约 120 行，包含 `AboutScreen`、`AboutInfoRow` 组件，使用深色主题配色。）

- [ ] **Step 2: 提交**

```bash
git add app/src/main/java/com/glut/schedule/ui/pages/AboutScreen.kt
git commit -m "feat: create AboutScreen extracted from ScheduleScreen"
```

---

### Task 8: 创建 DirectLoginScreen 和 DirectLoginViewModel

**Files:**
- Create: `app/src/main/java/com/glut/schedule/ui/pages/DirectLoginScreen.kt`
- Create: `app/src/main/java/com/glut/schedule/ui/pages/DirectLoginViewModel.kt`

- [ ] **Step 1: 创建 DirectLoginViewModel**

复用 `AcademicLoginService` + `AcademicLoginHttpClient` 进行直接 POST 登录。登录成功后调用 `ApiProbeService` 抓取课表/考试/成绩全部数据，一并存入 Room。

（约 220 行，包含：验证码加载、登录状态管理、登录后数据导入流程。）

- [ ] **Step 2: 创建 DirectLoginScreen UI**

登录表单页面：学号 + 密码 + 验证码图片 + 记住密码 + 登录按钮 + 导入结果卡片（课程/考试/成绩数量）。

（约 180 行 Compose UI。）

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/glut/schedule/ui/pages/DirectLoginViewModel.kt app/src/main/java/com/glut/schedule/ui/pages/DirectLoginScreen.kt
git commit -m "feat: create DirectLoginScreen to replace WebView import"
```

---

### Task 9: 创建 ScoreScreen 和 ScoreViewModel

**Files:**
- Create: `app/src/main/java/com/glut/schedule/ui/pages/ScoreScreen.kt`
- Create: `app/src/main/java/com/glut/schedule/ui/pages/ScoreViewModel.kt`

- [ ] **Step 1: 创建 ScoreViewModel**

管理成绩数据流，刷新时从教务 `studentOwnScore.do` 获取多个学期的成绩，通过 `ScoreParser` 解析后存入 Room。

（约 120 行。）

- [ ] **Step 2: 创建 ScoreScreen UI**

成绩列表页面：顶部标题 + 刷新按钮 + 平均绩点/总学分汇总 + 按学期自动分组 + 成绩行（课程名/分数/绩点/学分）+ 绩点颜色编码。

（约 160 行 Compose UI。）

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/glut/schedule/ui/pages/ScoreViewModel.kt app/src/main/java/com/glut/schedule/ui/pages/ScoreScreen.kt
git commit -m "feat: create ScoreScreen and ScoreViewModel"
```

---

### Task 10: 重构 MainActivity — Navigation Drawer

**Files:**
- Modify: `app/src/main/java/com/glut/schedule/MainActivity.kt`
- Modify: `app/src/main/java/com/glut/schedule/ScheduleApplication.kt`

- [ ] **Step 1: 更新 AppContainer 添加新依赖**

在 `ScheduleApplication.kt` 的 `AppContainer` 中添加：
```kotlin
val scoreParser = ScoreParser()
val updateChecker = UpdateChecker()
```

- [ ] **Step 2: 重写 MainActivity**

将 `sealed class MainScreen` 替换为 `ModalNavigationDrawer`：
- `rememberDrawerState` 管理抽屉状态
- `ModalDrawerSheet` 渲染抽屉菜单（DrawerHeader + LazyColumn 菜单项）
- `when(selectedItem)` 渲染内容区
- TopAppBar 左侧汉堡菜单图标
- 启动时检查更新，`UpdateDialog` 弹窗
- 移除所有 WebView 相关代码

（约 280 行，包含 `MainActivity`、`DrawerHeader`、`DrawerMenuItem`、`SettingsPage`、`UpdateDialog` 组件。）

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/glut/schedule/MainActivity.kt app/src/main/java/com/glut/schedule/ScheduleApplication.kt
git commit -m "feat: refactor to ModalNavigationDrawer layout"
```

---

### Task 11: 简化 ScheduleScreen + 增强刷新反馈

**Files:**
- Modify: `app/src/main/java/com/glut/schedule/ui/pages/ScheduleScreen.kt`
- Modify: `app/src/main/java/com/glut/schedule/ui/pages/ScheduleViewModel.kt`

- [ ] **Step 1: 移除内嵌弹窗**

从 `ScheduleScreen` 中移除：
- `ScheduleSettingsPanel`（已独立为 Drawer 的 Settings 页面）
- `ScheduleAddActionsPanel`（移入 Drawer 的 Settings 页面处理背景设置）
- `AboutScheduleDialog`（已独立为 Drawer 的 About 页面）

保留：星空调度背景、ScheduleGrid、Snackbar 消息显示。

- [ ] **Step 2: 增强刷新反馈**

在 `ScheduleViewModel.refreshSchedule()` 开头记录旧课程数：
```kotlin
val oldCourseCount = repository.courses.first().size
```
成功导入后对比：
```kotlin
val newCount = courses.size
message.value = if (newCount != oldCourseCount)
    "课表已更新: $oldCourseCount → $newCount 门课程"
else
    "课表未发生变化 ($newCount 门课程)"
```

- [ ] **Step 3: 提交**

```bash
git add app/src/main/java/com/glut/schedule/ui/pages/ScheduleScreen.kt app/src/main/java/com/glut/schedule/ui/pages/ScheduleViewModel.kt
git commit -m "feat: simplify ScheduleScreen, add change detection to refresh"
```

---

### Task 12: 移除 WebView 相关文件

**Files:**
- Remove: `app/src/main/java/com/glut/schedule/ui/pages/AcademicImportScreen.kt`
- Remove: `app/src/main/java/com/glut/schedule/ui/pages/AcademicImportViewModel.kt`
- Remove: `app/src/main/java/com/glut/schedule/service/academic/AcademicWebScripts.kt`
- Remove: `app/src/main/java/com/glut/schedule/service/academic/AndroidCredentialCapture.kt`
- Remove: `app/src/main/java/com/glut/schedule/service/academic/DebugCaptureService.kt`

- [ ] **Step 1: 删除并提交**

```bash
git rm app/src/main/java/com/glut/schedule/ui/pages/AcademicImportScreen.kt
git rm app/src/main/java/com/glut/schedule/ui/pages/AcademicImportViewModel.kt
git rm app/src/main/java/com/glut/schedule/service/academic/AcademicWebScripts.kt
git rm app/src/main/java/com/glut/schedule/service/academic/AndroidCredentialCapture.kt
git rm app/src/main/java/com/glut/schedule/service/academic/DebugCaptureService.kt
git commit -m "chore: remove WebView-based import files"
```

---

### Task 13: 版本升级、构建与测试

**Files:**
- Modify: `app/build.gradle.kts`

- [ ] **Step 1: 升级版本号**

```kotlin
versionCode = 26
versionName = "0.6.0"
```

- [ ] **Step 2: 构建 Debug APK**

```powershell
$env:JAVA_HOME = "C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot"
.\gradlew.bat assembleDebug
```

- [ ] **Step 3: 运行单元测试**

```powershell
.\gradlew.bat testDebugUnitTest
```

- [ ] **Step 4: 修复编译/测试错误**

- [ ] **Step 5: 构建 Release APK**

```powershell
.\gradlew.bat assembleRelease
```

- [ ] **Step 6: 提交**

```bash
git add app/build.gradle.kts
git commit -m "release: bump version to 0.6.0 (versionCode 26)"
```

---

## 验证清单

1. `.\gradlew.bat assembleDebug` 构建成功
2. `.\gradlew.bat testDebugUnitTest` 测试通过
3. 6 个抽屉菜单项正确切换
4. 直接登录表单 → 导入课表/考试/成绩
5. 成绩列表显示，绩点颜色编码正确
6. 启动时检查更新，弹窗提示
7. 刷新按钮 Snackbar 显示变化结果
